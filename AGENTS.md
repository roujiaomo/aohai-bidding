# 全局变更与规则同步约束

本项目以 `docs/整体程序规则.md`（下称“规则文件 A”）作为面向业务、运营和验收的整体程序规则说明。

任何功能改动在编码前，实施 Agent 必须自行逐项检查：是否改变以下任一项。该检查是 Agent 的职责，不能把判断问题抛给需求方，也不能因未收到逐项确认而跳过。

1. 数据抓取、详情补全、入库、去重、时效或回收站规则；
2. 评分、优先级、直接商机/市场情报/AI 排除的分类规则；
3. AI 输入、事实抽取、程序裁决、失败降级或人工确认规则；
4. 实时商机、历史商机、回收站、钉钉通知或帮助页的显示/推送规则；
5. 状态机、审计、质量报告、异常告警或历史重审行为。

任一项发生变化时，提交必须同时完成：

- 更新 `docs/整体程序规则.md`；
- 如机器裁决或数据规则有变化，提升 `app/governance.py` 的 `RULEBOOK_VERSION`；
- 更新或新增固定回归样本；
- 运行 `python scripts/verify_rule_document.py` 和 `python scripts/governance_tools.py release-gate`；
- 对分支发布额外运行 `python scripts/change_impact_check.py --base <发布基线>`；
- 在交付说明中写明历史数据影响、未来数据影响、回退点和验证结果。

所有新增、删除或修改功能（包括仅 UI、定时任务、脚本和运维入口）在交付前还必须完成以下两项，不得以“代码未报错”替代：

- **自测**：至少覆盖正常路径、与本次变更直接相关的边界/失败路径，以及已有功能不回归；规则行为变更必须执行固定回归样本和发布门禁。无法自动化的界面或外部链路，必须记录人工验证步骤和结果。
- **历史数据处置判断**：明确给出“无需处理 / 只读审计或预览 / 需要修复或重评”三选一结论，并说明判断依据、受影响记录范围、是否会改变原始数据或仅改变派生状态。若需要写入历史数据，必须先备份、先跑只读预览、保留审计快照和回退点，再执行并复核数量；人工最终结论不得被自动覆盖。

交付说明必须单列“自测结果”和“历史数据处置”两节；没有历史影响时，也必须明确写“无需处理”及原因。

不允许在未更新规则文件 A 和回归样本的情况下发布规则行为变化。纯样式、文案、日志或不改变任何上述行为的重构，可在交付说明中标记为“无规则变化”。实施 Agent 必须在交付中说明检查结论及同步结果；只有确实会改变用户可见或数据处理行为时才需要让需求方确认发布范围。
